# --- Privat-Wand (ab Tag 1) — persoenliche Denk-Dateien nie in die Historie ---
brain/users/*/journal/
brain/users/*/sessions/
brain/users/*/avatar.md

# --- Maschinen-lokale Verdrahtung (pro Rechner, nie geteilt) ---
.claude/settings.local.json
.claude/local-user.md
*.local.json
# Vorlage MUSS in die Historie (sonst kein Muster fuer neue Rechner) — vom Muster oben ausnehmen
!.claude/VORLAGE-settings.local.json

# --- Kunden-Rohexporte (enthalten Endkunden-Daten: Name, Mail, Adresse, Telefon) ---
# Nie in die Historie. Auswertungen ja, Rohdaten nein. Siehe
# brain/lessons/system/2026-07-22-endkundendaten-in-kunden-exporten.md
kunden/**/*.csv
kunden/**/orders_export*
kunden/**/customers_export*

# --- Muell / Build-Artefakte ---
node_modules/
__pycache__/
.env

~$*.pptx

kunden/finelli/finadmin/server/node_modules/
kunden/finelli/finadmin/server/.wrangler/
